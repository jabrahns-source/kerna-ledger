#!/usr/bin/env python3
"""
This file is an intentional redirect stub.

The canonical, fully functional QRegEngine (deterministic gates, Ed25519,
Merkle chaining, test suite, verifier) lives in:

    https://github.com/jabrahns-source/Q-Reg

Clone and run that repository:

    python qreg_engine.py --demo

Do not use any code from this path for production or verification.
This umbrella repo (kerna-ledger) retains the stub solely for discoverability
and to avoid broken imports in older references.
"""

raise SystemExit(
    "qreg_engine has moved to the canonical location. "
    "Use https://github.com/jabrahns-source/Q-Reg (python qreg_engine.py --demo)"
)
